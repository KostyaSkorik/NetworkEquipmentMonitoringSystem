services:
  db:
    image: postgres:15-alpine
    container_name: network_monitor_db
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: admin
      POSTGRES_DB: net_metrics
    ports:
      - "5430:5432"
    volumes:
      - postgres_data:/var/lib.postgresql/data
volumes:
  postgres_data: